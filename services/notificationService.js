"use strict";

const {
  User,
  Asset,
  MaintenanceSchedule,
} = require("../models");
const { Op } = require("sequelize");
const logger = require("../utils/logger");

/**
 * Notification Service
 * Handles notification checking
 */
class NotificationService {
}

module.exports = NotificationService;
